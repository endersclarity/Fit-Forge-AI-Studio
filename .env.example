# FitForge Local - Environment Configuration Example
# Copy this file to backend/.env.local for local npm development

# =============================================================================
# PORT CONFIGURATION
# =============================================================================

# Backend API Port
# - Docker: Uses 3001 (defined in docker-compose.yml)
# - Local npm dev: Use 3002 to avoid Docker port conflicts (set in backend/.env.local)
PORT=3001

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Database file path
# - Docker: /data/fitforge.db (container path)
# - Local npm dev: ../data/fitforge.db (relative to backend/)
DB_PATH=../data/fitforge.db

# =============================================================================
# PORT REFERENCE TABLE
# =============================================================================
#
# Startup Method  | Frontend     | Backend      | Notes
# ----------------|--------------|--------------|---------------------------
# Docker          | 3000         | 3001         | Recommended method
# npm dev         | 3000 (Vite)  | 3002         | Set in backend/.env.local
#
# =============================================================================
